@model List<WebForLink.Web.ViewModels.EnderecoVM>
<div class="row" style="padding-left: 20px; padding-right:20px; padding-top:10px;" id="lstEnderecoFicha">
    @for (int i = 0; i < Model.Count; i++)
    {
        @Html.EditorFor(model => Model[i])
    }
</div>
